import { ResolvedPipe } from '../shared/resolved.pipe';

describe('ResolvedPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolvedPipe();
    expect(pipe).toBeTruthy();
  });
});
